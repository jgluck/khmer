#include "cyclic-hash.hh"


unsigned long long hash_it = hash(const char * kmer, const long k_len, 
        unsigned long long h)
{
    for (WordLength i = 1, j = k - 2; i < k; i++, j--) {

    unsigned long long hash_return = md5(h);
}

void main()
{
    unsigned long long h = 0, r = 0; 

